/**
 * @id EXP_07_05_01_006_PASS_TUPLE_CONTEXT
 * @expect compile-pass
 * @section 7.5.1
 * @design Tuple 类型上下文：let tuple: [number, string] = [1, "hello"]
 * @note 正向用例：验证 tuple 类型作为上下文编译通过，元素数与类型数匹配
 */
let tuple: [number, string] = [1, "hello"]
