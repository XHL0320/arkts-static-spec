/**
 * @id EXP_07_05_01_007_PASS_FIXED_ARRAY_CONTEXT
 * @expect compile-pass
 * @section 7.5.1
 * @design FixedArray 类型上下文：let a: FixedArray<string> = ["hello", "world"]
 * @note 正向用例：验证 FixedArray 类型作为上下文编译通过
 */
let a: FixedArray<string> = ["hello", "world"]
