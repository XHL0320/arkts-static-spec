/**
 * @id GEN_05_03_027_FAIL_DEFAULT_ORDER_IMPLICIT
 * @expect compile-fail
 * @section 5.3
 * @cross-language AMBIGUITY 跨语言歧义用例
 * @design 有默认值的类型参数后跟无默认值参数的隐式实例化
 * @note 反向用例：验证隐式实例化时，有默认值的参数后面有无默认值参数，
 *       是否影响类型参数推断。
 *
 * SPEC EXPECTS: 5.1.2 行217-220 — 有默认值的类型参数后不能有无默认值参数。
 *               5.2.2 行732,746 — 非 well-formed 实例化应报错。
 *
 * CROSS-LANGUAGE COMPARISON (ArkTS / Java / Swift):
 * ┌───────────┬──────────────────────────────────────────┐
 * │ ArkTS     │ 声明时检查：有默认后不可接无默认。         │
 * │           │   隐式实例化时仍需满足此约束。              │
 * ├───────────┼──────────────────────────────────────────┤
 * │ Java      │ 不支持类型参数默认值。                    │
 * ├───────────┼──────────────────────────────────────────┤
 * │ Swift     │ 支持默认值，但约束：有默认值后所有参数      │
 * │           │   都需有默认值（与 ArkTS 相同）。          │
 * └───────────┴──────────────────────────────────────────┘
 *
 * GAP ANALYSIS:
 *   类型 3（推断消解约束检查）— 隐式实例化时，Checker 的推断优先级
 *   是否会在未完成默认值检查前就推断出类型，从而绕过顺序约束？
 *   若推断在约束检查之前完成 → 可能绕过默认值顺序错误。
 */
class C<T1, T2 = number, T3> {
    value1: T1;
    value2: T2;
    value3: T3;
}
// 隐式实例化：T2 有默认值，T3 无默认值 → 违反顺序约束
// 编译器应在声明 C 时即报错，不论是否实例化
