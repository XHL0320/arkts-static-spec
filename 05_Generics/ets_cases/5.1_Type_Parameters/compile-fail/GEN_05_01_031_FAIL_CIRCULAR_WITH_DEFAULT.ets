/**
 * @id GEN_05_01_031_FAIL_CIRCULAR_WITH_DEFAULT
 * @expect compile-fail
 * @section 5.1
 * @design 默认值参与循环依赖检测的边界
 * @note 反向用例：验证类型参数默认值解析与循环依赖检测的相位关系。
 *       若默认值解析在循环检测之前 → T1=T2, T2 extends T1 被检测为循环。
 *       若循环检测在默认解析之前 → T1 未解析默认时可能漏检。
 *
 * SPEC EXPECTS: 5.1.1 行197-203：类型参数直接或间接依赖自身应报错。
 *               T2 extends T1，T1 默认 = T2，形成间接循环。
 *
 * CROSS-LANGUAGE COMPARISON (ArkTS / Java / Swift — 两阶段声明歧义):
 * ┌───────────┬──────────────────────────────────────────┐
 * │ ArkTS     │ 两阶段：先注册名再解析约束。默认值解析     │
 * │           │   时机决定循环是否被检测。                  │
 * ├───────────┼──────────────────────────────────────────┤
 * │ Java      │ 类型参数不支持默认值，此场景不存在。        │
 * ├───────────┼──────────────────────────────────────────┤
 * │ Swift     │ 支持默认值，但约束只能引用前面的参数。      │
 * │           │   不允许 T2 extends T1 且 T1 默认引用 T2。 │
 * └───────────┴──────────────────────────────────────────┘
 *
 * GAP ANALYSIS:
 *   类型 6（两阶段声明消解循环依赖）— Binder 先注册名再解析类型。
 *   默认值 T1=T2 的解析时机（Binder 阶段 vs Checker 阶段）可能影响
 *   循环依赖检测的结果。
 *   若默认值在 Checker 阶段解析 → Binder 阶段看不到 T1→T2 的边
 *   → 循环可能漏检。
 */
class F<T1 = T2, T2 extends T1> {
    value: T1;
}
