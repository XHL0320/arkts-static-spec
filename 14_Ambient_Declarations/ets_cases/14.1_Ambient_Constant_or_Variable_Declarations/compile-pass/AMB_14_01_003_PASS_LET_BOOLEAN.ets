/**
 * @id AMB_14_01_003_PASS_LET_BOOLEAN
 * @expect compile-pass
 * @section 14.1
 * @design declare let 声明 boolean 类型，显式类型注解，无初始化器
 * @note 正向用例：验证 ambient let 声明 boolean 类型编译通过
 */
declare let v3: boolean
