# 4.1 Names - ArkTS 与 Java/Swift/TS 行为差异及规范一致性报告

**报告日期：** 2026-06-22  
**测试用例数：** 16（8 compile-pass + 6 compile-fail + 2 runtime）  
**目的：** 通过用例执行（编译期 + 真实运行时）+ 跨语言对比，识别 ArkTS 命名规则的设计问题

---

## 报告分类口径

| 分类 | 含义 | 处理方式 |
|------|------|----------|
| 符合 ArkTS spec 的语言设计差异 | 行为与 Java/Swift 不同，但符合 ArkTS spec 或当前明确语义 | 不标为缺陷，仅记录差异 |
| Spec 与实现不一致 | 用例与 spec 要求不一致，且当前实现未按 spec 报错/运行 | 保留 FAIL 用例并记录 issue_report |
| 待确认问题 | 需要补充 stdlib/spec/实现依据后才能定性 | 暂不判定为缺陷 |
| 已验证规范一致行为 | 用例验证 ArkTS 行为符合 spec | 记录为通过项 |

---

## 一、行为差异与规范一致性概览

### 问题 A-4.1：保留名清单不完整 ⭐⭐ MEDIUM

**用例：** NAM_04_01_015_FAIL_TYPE_KEYWORD

**实际行为：**
```typescript
let int: number = 1;  // ❌ Semantic error: 'int' is a reserved name
let string: string = "hello";  // ❌ Semantic error
```

**Spec 依据：** §4.2 行 179-191 仅列出 `number`、`String`、`Record`、`Object`、`Array` 作为预定义类型名冲突的示例，未完整列举所有保留名。

**对比：**
| 语言 | 保留名管理 |
|------|-----------|
| ArkTS | 编译器保留 `int`、`string`、`Box` 等，但 Spec 未完整列举 |
| Java | JLS 明确定义所有关键字和保留名 |
| Swift | 关键字可用反引号转义，保留名管理灵活 |
| TypeScript | 部分关键字可作变量名 |

**影响：** 用户不知道哪些名称不能使用，只能依赖编译器报错。

**建议：** Spec 应完整列举所有保留名，或引用独立的保留名清单文档。

---

### 问题 B-4.1：关键字作为名称的限制 ⭐ LOW

**Observation:** ArkTS 不允许使用关键字作为标识符（如 `let`、`class`、`function`），而 Swift 允许使用反引号转义关键字作为标识符。

**对比：**
| 语言 | 关键字作为标识符 |
|------|----------------|
| ArkTS | ❌ 编译拒绝 |
| Java | ❌ 编译拒绝 |
| Swift | ✅ 反引号转义（如 `` `class` ``） |
| TypeScript | ✅ 部分关键字可作变量名 |

**影响：** 限制了命名灵活性，但符合静态语言设计哲学。

**评价：** 符合 ArkTS 简化设计哲学，非缺陷。

---

## 二、跨语言对比详细分析

### 2.1 标识符规则

| 维度 | ArkTS | Java | Swift | TypeScript |
|------|-------|------|-------|------------|
| 合法字符 | Unicode字母/数字/\_/\$ | Unicode字母/数字/\_/\$ | Unicode字母/数字/\_ | Unicode字母/数字/\_/\$ |
| 关键字限制 | ❌ 不可用作标识符 | ❌ 不可用作标识符 | ✅ 反引号转义 | ✅ 部分关键字可作变量名 |
| 数字开头 | ❌ 编译拒绝 | ❌ 编译拒绝 | ❌ 编译拒绝 | ❌ 编译拒绝 |
| 空名称 | ❌ 编译拒绝 | ❌ 编译拒绝 | ❌ 编译拒绝 | ❌ 编译拒绝 |

### 2.2 限定名规则

| 维度 | ArkTS | Java | Swift | TypeScript |
|------|-------|------|-------|------------|
| 模块/包级 | 模块.成员 | 包.类.成员 | 模块.类型.成员 | 模块.成员 |
| 实例成员 | this.成员 | this.成员 | self.成员 | this.成员 |
| 静态成员 | 类名.成员 | 类名.成员 | 类型名.成员 | 类名.成员 |

---

## 三、已验证 ArkTS 行为（与规范一致）

| 行为 | 用例 | 状态 |
|------|------|------|
| 简单名称声明和引用 | NAM_04_01_001 | ✅ |
| 模块限定名 | NAM_04_01_002 | ✅ |
| 类限定名 | NAM_04_01_003 | ✅ |
| 实例限定名 | NAM_04_01_004 | ✅ |
| 下划线标识符 | NAM_04_01_005 | ✅ |
| 美元符号标识符 | NAM_04_01_006 | ✅ |
| 多层级限定名 | NAM_04_01_007 | ✅ |
| 接口类型变量限定名访问实例成员 | NAM_04_01_008 | ✅ |

---

## 四、严重性等级总览

| 严重性 | 数量 | 问题列表 |
|-------|------|---------|
| ⭐⭐⭐ HIGH | 0 | 无 |
| ⭐⭐ MEDIUM | 1 | 问题 A-4.1 保留名清单不完整 |
| ⭐ LOW | 1 | 问题 B-4.1 关键字作为名称的限制 |

---

## 五、4.1 章节核心结论

| 维度 | 评价 |
|------|------|
| 标识符规则 | ⭐⭐⭐⭐ 完整支持，与主流语言一致 |
| 关键字限制 | ⭐⭐⭐ 严格但合理，Swift 更灵活 |
| 限定名支持 | ⭐⭐⭐⭐⭐ 完整支持模块/类/实例三级 |
| Spec 完整性 | ⭐⭐ 保留名清单未完整列举 |

---

## 六、建议改进

### 短期
1. Spec 完整列举所有保留名，或引用独立的保留名清单文档

### 中期
2. 评估是否允许反引号转义关键字作为标识符（提升 Swift 兼容性）

### 长期
3. 考虑更灵活的命名规则（如 TypeScript 的部分关键字作变量名）
