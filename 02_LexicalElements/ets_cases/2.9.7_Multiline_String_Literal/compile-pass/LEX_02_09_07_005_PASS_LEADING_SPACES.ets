/**
 * @id LEX_02_09_07_005_PASS_LEADING_SPACES
 * @expect compile-pass
 * @section 2.9.7
 * @design 前导空格保留
 * @note compile-pass 用例：验证前导空格保留
 */

// 前导空格保留
let s1 = `  Leading spaces are preserved`
let s2 = `    Four spaces`
