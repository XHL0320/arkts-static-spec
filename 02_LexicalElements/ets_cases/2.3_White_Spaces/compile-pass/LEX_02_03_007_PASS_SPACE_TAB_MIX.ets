/**
 * @id LEX_02_03_007_PASS_SPACE_TAB_MIX
 * @expect compile-pass
 * @section 2.3
 * @design Space + Tab 混合分隔Token * @note 正向用例：验证该特性编译通过的正确用法

 */
function testSpaceTabMix(): void {
  let 	x:	 number 	=	 1
  let	 	y: number	= 2
}
