/**
 * @id LEX_02_03_002_PASS_TAB_SEPARATOR
 * @expect compile-pass
 * @section 2.3
 * @design Horizontal tabulation (U+0009) 作为词法分隔符 * @note 正向用例：验证该特性编译通过的正确用法

 */
function testTabSeparator(): void {
	let	x:	number	=	1
	let	y:	number	=	2
}
