/**
 * @id LEX_02_09_015_PASS_BIGINT_INFER
 * @expect compile-pass
 * @section 2.9.1
 * @design bigint 类型推断
 * @note compile-pass 用例：验证 bigint 类型推断
 */

// bigint 字面量推断
let x = 123n
let y = 0n
let z = 9876543210n
