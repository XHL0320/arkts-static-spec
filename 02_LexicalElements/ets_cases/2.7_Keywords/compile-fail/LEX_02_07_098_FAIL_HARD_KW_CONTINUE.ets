/**
 * @id LEX_02_07_098_FAIL_HARD_KW_CONTINUE
 * @expect compile-fail
 * @section 2.7
 * @design 硬关键字 continue 不能用作标识符
 * @note spec: hard keywords cannot be used as identifiers
 */
let continue: int = 1
