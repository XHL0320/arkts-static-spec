/**
 * @id LEX_02_07_091_FAIL_HARD_KW_ABSTRACT
 * @expect compile-fail
 * @section 2.7
 * @design 硬关键字 abstract 不能用作标识符
 * @note spec: hard keywords cannot be used as identifiers
 */
let abstract: int = 1
